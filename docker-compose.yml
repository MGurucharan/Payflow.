version: '5.5.0'
services:
  postgres:
    container_name: container-pg
    image: postgres
    hostname: localhost
    ports: 
      - "8001:5432"
    environment:
      POSTGRES_USER: guru
      POSTGRES_PASSWORD: gurucharan
      ConnectionStrings:DefaultConnection: "Host=localhost;Database=guru;Username=guru;Password=gurucharan"
      
    volumes:
      - local_pgdata:/var/lib/postgresql
    restart: always

  pgadmin:
    container_name: container-pgadmin
    image: dpage/pgadmin4
    ports:
      - "5050:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: user-name@domain-name.com
      PGADMIN_DEFAULT_PASSWORD: strong-password
    restart: always

volumes:
  postgres-data:
  local_pgdata:
  